
tasks:
  Run Task:
    shell: python -m robocorp.tasks run tasks_producer.py produce_workitems
    shell: python -m robocorp.tasks run tasks.py scraper

environmentConfigs:
  - environment_windows_amd64_freeze.yaml
  - conda.yaml

artifactsDir: output/downloads

PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore